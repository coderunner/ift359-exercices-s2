//SECTION 4 - Jouer!
/**
import { afficherPartie, ciseaux, Contexte, jouerPartie, Joueur, roche } from "./rps.js";

export const j1: Joueur = {
  nom: "J1",
  strategie: (contexte: Contexte) => roche("Ouch"),
};

export const j2: Joueur = {
  nom: "J2",
  strategie: (contexte: Contexte) => ciseaux("Arg!!!"),
};

const partieJouee = jouerPartie(j1, j2, 10);
afficherPartie(partieJouee);
*/
